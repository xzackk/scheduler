services:
  scheduler:
    image: ${IMAGE:-ghcr.io/CHANGE_ME/scheduler:latest}
    container_name: scheduler
    restart: unless-stopped
    ports:
      - "3005:3005"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./index.html:/usr/share/nginx/html/index.html:ro
    labels:
      - "wud.watch=true"